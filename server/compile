#!/bin/bash

# Compile server alone:
# closure-compiler --flagfile compile.flag --entry_point=codecity.js

# Compile tests, which also compile server:
closure-compiler --flagfile compile.flag --entry_point=tests/run.js tests/**.js
